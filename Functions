var is the variable in which we can store some information 
like floating number or Integers or Strings etc.
var 1 is the input which is typecasted to integers.
var 2 is also an integer is the input which is typecasted to integers.
+ function adds those both input and give the result.

